help(read.csv)
cb = read.csv("Cracker Barrel .csv" header=T, sep=";")
cb = read.csv("Cracker Barrel.csv" header=T, sep=";")
cb = read.csv("Cracker\ Barrel\.csv" header=T, sep=";")
help(read.csv)
cb = read.csv("Cracker Barrel .csv", header=T, sep=";")
ob = read.csv("2008_estimated_obesity_by_us_county.csv", header=T, sep=",")
cb
ob
plot(cb$LAT, cb$LONG)
data = cb$LAT
data <- cb$LAT
base <- data[3:8]
base.r <- abs(cor(base))
help(dmat.color)
man(install)
help(install)
install.packages(dmat)
install.packages("dmat")
help(plot)
help(contour)
x <- ob$lat
y <- ob$long
z <- ob$age_adjusted_percent
image.maker <- function(coords, value){
  N <- length(unique(coords[,1]))
  image.out <- matrix(NA, nrow = N, ncol = N)
  coords[,1] <- as.numeric(factor(coords[,1]))
  coords[,2] <- as.numeric(factor(coords[,2]))
  for (i in 1:nrow(coords))
    image.out[coords[i,1], coords[i,2]] <- value[i]
  return(image.out)
}
my.image <- image.maker(c(x,y), z)
x
y
len(x)
length(x)
length(y)
x
history
history()
my.image <- image.maker(ob[c("lat", "long")], z)
contour(my.image)
my.image
contour(my.image)
help(contour)
help(seq)
levels <- seq(20,40,.05
)
levels
levels <- seq(25,40,.5)
contour(my.image, levels=levels)
